# Session Notes

Scratch space for carrying context between agent sessions — not a permanent record. If something here matters long-term, it belongs in a real doc (`testing.md`, `ci.md`, an ADR, a GitHub issue) instead of living here indefinitely.

**Lifecycle**: cleared back to empty at the start of every session. Written to only at the end of a session, only when the user explicitly asks for it — never automatically.

## 2026-08-07

- **#29 (Session Logging) and #30 (Bodyweight Logging) both shipped, merged, closed.** MVP path now: #31 Progress Charts next (feeds off #29's `one_rm_estimates` + #30's `bodyweight_entries`, both now populated by real routes). #32 (Theming) still independent/open, see below — its scope grew.
- **New doc**: `docs/engineering/known-issues.md`, wired into `CLAUDE.md`'s index. First entry: styling has no design system and isn't theme-aware — every route (`programs/`, `log/`, `bodyweight/`) uses hardcoded inline hex, `globals.css`'s `prefers-color-scheme` approach doesn't match #32's resolved plan (user-picked roles via `next-themes`, not OS auto-detect). #32 will need a real retrofit pass, not just "two JSON files and a toggle" — read that doc before picking up #32.
- **Recurring Prettier/git-status noise is NOT resolved**, despite the `.gitattributes` fix (`* text=auto eol=lf`) added mid-session during #29. It recurred 4+ more times _after_ that fix — every commit this session needed a `git checkout -- <noise files>` sweep before committing. Always the same shape: pure reformatting (quote style, line-wrapping, blank-line insertion) across files nobody touched (`db/schema.ts`, `docker-compose.yml`, `.claude/skills/*`, etc.), never real content changes. Root cause still unconfirmed — asked the user to check WebStorm's Settings → Tools → Actions on Save (Reformat code / Run on save scope), but never got confirmation they checked. **Worth moving this into `known-issues.md` next session** rather than re-diagnosing from scratch — it's clearly session-spanning now, not a one-off.
- **Browser automation (claude-in-chrome) disconnected mid-session** and never reconnected despite retries — used it successfully early on (live-tested #29 through the real UI, screenshots, form filling) but lost it partway through and finished #30's verification via curl + direct DB queries instead. May just need the extension restarted next session.
- **Local dev env still running** at end of session: `pnpm --filter web dev` on port 3000 (PID 46592), and the `docker-compose` Postgres container (`liftingtracker-postgres-1`, up 8h). Dev DB has the seeded exercises plus a manually-created "5 3 1" test program (Squat/Bench days) used to live-test #29's Wave scheme end-to-end — harmless leftover, fine to ignore or delete.
