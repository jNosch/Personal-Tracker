# Visual references

Diagrams and other visual artifacts from planning/engineering sessions — Mermaid where possible so they render directly on GitHub, kept as hand-maintained snapshots rather than generated output.

- [`erd.md`](./erd.md) — current database schema.
- [`architecture.md`](./architecture.md) — current app/data-flow architecture.

Built with help from `/graphify` (knowledge-graph extraction over the repo) as a research pass, then hand-drawn against `apps/web/db/schema.ts` directly for exact fidelity — `/graphify`'s own output (`graphify-out/`) is a regeneratable local cache, not committed here.
